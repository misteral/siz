<%= form_for(@dolgnost) do |f| %>
  <% if @dolgnost.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@dolgnost.errors.count, "error") %> prohibited this dolgnost from being saved:</h2>

      <ul>
      <% @dolgnost.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :prior %><br />
    <%= f.number_field :prior %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

<h2> <%= @rabotnik.rabotnik_fio_tn %></h2>
<hr>
<div class="row">
  <div class="span4">
    <p><b>Отдел:</b> <%= @rabotnik.otdel[:name] %></p>
    <p><b>Должность</b> <%= @rabotnik.dolgnost[:name] %></p>
  </div>
  <div class="span4">
    <p><b>Рост: </b> <%= @rabotnik.rost[:name] %></p>
    <p><b>Размер одежды: </b> <%= @rabotnik.razmer_od[:name] %></p>
    <p><b>Размер обуви: </b> <%= @rabotnik.razmer_o[:name] %></p>
  </div>
</div>

<div class="form-actions">
  <%= link_to 'Back', rabotniks_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_rabotnik_path(@rabotnik), :class => 'btn' %>
  <%= link_to 'Delete', rabotnik_path(@rabotnik), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>

<hr>
<div class="tabbable">
  <ul class="nav nav-tabs">
    <li class="active"><a href="#1" data-toggle="tab">Предусмотрено по типовым нормам</a></li>
    <li><a href="#2" data-toggle="tab">Выдано на текущий момент</a></li>
  </ul>
  <div class="tab-content">
    <div class="tab-pane active" id="1">
      <table class="table table-striped table-bordered table-condensed">
       <thead class="table-centr">
        <tr>
          <th>Наименование средств индивидуальной защиты</th>
          <th>Единица измерения</th>
          <th>Количество на год</th>
        </tr>
        </thead>
       <tbody>
       <% @rabotnik.pologenos.each do |pol| %>
        <tr>
        <td><%= Siz.find(pol['siz_id']).name %></td>
        <td><%= Ed.find(pol['ed_id']).name %></td>
        <td><%= pol['nagod'] %></td>
        </tr>
        <% end %>
        </tbody>
     </table>
    </div>
    <div class="tab-pane" id="2">
      <p>Howdy, I'm in Section 2.</p>
    </div>
  </div>
</div>



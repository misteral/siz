<h1>/Ed</h1>

<p>
  <b>Name</b><br>
  <%= @ed.name %>
</p>

<div class="form-actions">
  <%= link_to 'Back', eds_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_ed_path(@ed), :class => 'btn' %>
  <%= link_to 'Delete', ed_path(@ed), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>

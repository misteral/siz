<h1>/Rost</h1>

<p>
  <b>Name</b><br>
  <%= @rost.name %>
</p>

<div class="form-actions">
  <%= link_to 'Back', rosts_path, :class => 'btn'  %>
  <%= link_to 'Edit', edit_rost_path(@rost), :class => 'btn' %>
  <%= link_to 'Delete', rost_path(@rost), :method => 'delete', :confirm => 'Are you sure?', :class => 'btn btn-danger' %>
</div>
